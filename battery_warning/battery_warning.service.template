[Unit]
Description=Warn if the battery capacity drops too low

[Service]
User={{user}}
Environment=DISPLAY={{display}}
ExecStart=/usr/local/bin/battery_warning 0 15 300
Restart=on-abnormal

[Install]
WantedBy=default.target
